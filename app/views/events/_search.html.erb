<%= form_tag :search_events, { method: 'get', remote: true} do %>
<%= text_field_tag 'search_terms', (@previous_search.nil? ? "" : "#{@previous_search}"), placeholder: "Search"%>
<%= submit_tag "Search", id: "searchload" %>
<% end %>
